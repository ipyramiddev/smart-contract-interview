type User @entity {
    id: ID!
}

type Listing @entity {
    id: ID!
    seller: User
    buyer: User
    collection: NFTCollection
    tokenId: String
    price: BigInt
    status: String
    lastStatusTimestamp: BigInt
}

type NFTCollection @entity {
    id: ID!
    listings: [Listing!] @derivedFrom(field: "collection")
}
